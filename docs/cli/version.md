# `nika version`

Print Nika CLI and Go build information.

```bash
nika version
nika v
```

Both commands produce the same version information. The output includes the
Nika CLI version and, when available, the Go version from build metadata.
